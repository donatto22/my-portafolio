import './productCard.css'

const ProductCard = ({ title, description, image, webpage, glasmorphismClass }) => {
    return (
        <div className={`product ${glasmorphismClass}`}>

        </div>
    )
}

export default ProductCard
